<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RCCG COD Sunday School Attendance</title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-gray-100 min-h-screen">
    <%- include('partials/menu') %>
    <div class="container mx-auto px-4 py-8">
        <header class="mb-8">
            <h1 class="text-3xl md:text-4xl font-bold text-center text-primary">RCCG COD Sunday School Attendance</h1>
            <p class="text-center text-gray-600 mt-2">Track and manage Sunday School attendance</p>
        </header>
        
        <main>
            <div class="max-w-4xl mx-auto">
                <div class="bg-white rounded-lg shadow-lg p-6 mb-8">
                    <h2 class="text-2xl font-semibold mb-6 text-primary border-b pb-2">Select a Class</h2>
                    
                    <% if (classes && classes.length > 0) { %>
                        <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-4">
                            <% classes.forEach(cls => { %>
                                <a href="/class/<%= cls.id %>" class="block p-4 bg-gray-50 hover:bg-gray-100 rounded-lg border border-gray-200 transition-colors duration-200 transform hover:scale-105">
                                    <div class="flex justify-between items-center mb-2">
                                        <h3 class="text-xl font-semibold text-primary"><%= cls.name %></h3>
                                        <div class="bg-primary text-white rounded-full h-7 w-7 flex items-center justify-center font-bold text-sm">
                                            <%= cls.studentCount %>
                                        </div>
                                    </div>
                                    <div class="flex items-center mb-2">
                                        <svg class="w-4 h-4 text-primary mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
                                        </svg>
                                        <span class="text-gray-600 text-sm"><%= cls.studentCount %> Students</span>
                                    </div>
                                    <% if (cls.description) { %>
                                        <p class="text-gray-600 mt-1 text-sm"><%= cls.description %></p>
                                    <% } %>
                                </a>
                            <% }); %>
                        </div>
                    <% } else { %>
                        <div class="text-center py-8">
                            <p class="text-gray-600">No classes found.</p>
                            <p class="mt-2">Contact an administrator to add classes.</p>
                        </div>
                    <% } %>
                </div>
                
                <div class="bg-primary text-white rounded-lg shadow-lg p-6">
                    <h2 class="text-xl font-semibold mb-4">Quick Tips</h2>
                    <ul class="space-y-2">
                        <li class="flex items-start">
                            <svg class="h-6 w-6 mr-2 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <span>Click on a class to view and mark attendance</span>
                        </li>
                        <li class="flex items-start">
                            <svg class="h-6 w-6 mr-2 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <span>Attendance is automatically initialized when you open a class page</span>
                        </li>
                        <li class="flex items-start">
                            <svg class="h-6 w-6 mr-2 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                            </svg>
                            <span>View reports to see attendance statistics</span>
                        </li>
                    </ul>
                </div>
            </div>
        </main>
        
        <footer class="mt-12 text-center text-gray-500 text-sm">
            <p>&copy; 2025 RCCG COD Sunday School Attendance</p>
        </footer>
    </div>
</body>
</html>
